Number of registered users: <%= @users.count %><br>

  <% @users.each do |u| %>
  <li><%= u.username %> Total reviews: <%= u.reviews.count %>
  <%end%>
</ul><br>

Destination with most reviewed Books: <%= Destination.most_read_destination.name %>
